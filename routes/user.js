import express from "express";
const router = express.Router();

router.get("/user/:id", () => {});
router.get("/user-u/:username", () => {});
router.put("/user/:id", () => {});

export default router;
